apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: sensors
    user: sensor_user
    secureJsonData:
      password: sensor123
    jsonData:
      sslmode: disable
      postgresVersion: 1500
    isDefault: true
